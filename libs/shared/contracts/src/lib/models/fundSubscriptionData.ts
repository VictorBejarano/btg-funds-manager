export interface FundSubscriptionData {
    /**
     * El monto de la transacción
     */
    amount: number;
    /**
     * El identificador único del fondo
     */
    fundId: string;
    /**
     * El identificador único del usuario (UID)
     */
    userId: string;
}
