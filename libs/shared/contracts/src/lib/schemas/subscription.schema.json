{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "FundSubscription",
  "type": "object",
  "required": [
    "amount",
    "createdAt",
    "fundId",
    "fundMinInvestment",
    "fundName",
    "userId"
  ],
  "properties": {
    "amount": {
      "type": "integer",
      "minimum": 0,
      "description": "Monto invertido por el usuario."
    },
    "createdAt": {
      "type": "string",
      "format": "date-time",
      "description": "Fecha de creación en formato ISO 8601 (convertida desde Firestore Timestamp)."
    },
    "fundId": {
      "type": "string",
      "pattern": "^[a-zA-Z0-9]{20}$",
      "description": "ID único del documento del fondo."
    },
    "fundMinInvestment": {
      "type": "integer",
      "minimum": 0,
      "description": "Inversión mínima requerida capturada al momento de la suscripción."
    },
    "fundName": {
      "type": "string",
      "description": "Nombre del fondo para trazabilidad histórica."
    },
    "userId": {
      "type": "string",
      "description": "UID del usuario obtenido de Firebase Auth."
    }
  },
  "additionalProperties": false
}
